name: two_space_app
description: "A secure and feature-rich messenger for the TwoSpace ecosystem."
publish_to: 'none'
version: 0.1.6+12

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  
  # New libs
  animations: ^2.0.0
  sensors_plus: ^5.0.0
  flutter_contacts: ^1.1.9
  crypto: ^3.0.3

  # ===== State Management =====
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  riverpod: ^2.5.1

  # ===== Monitoring & Error Tracking =====
  sentry_flutter: ^8.14.2
  logger: ^2.4.0

  # ===== Security =====
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.2.0

  # ===== UI & Theming =====
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.1.0
  emoji_picker_flutter: ^2.2.0
  cached_network_image: ^3.3.1
  qr_flutter: ^4.1.0

  # ===== File & Path =====
  file_picker: ^8.0.6
  path_provider: ^2.1.3
  image_picker: ^1.1.2
  open_file: ^3.3.2

  # ===== Networking & API =====
  http: ^1.2.1
  dio: ^5.5.0+1
  connectivity_plus: ^6.0.3

  # ===== Database =====
  sqflite: ^2.3.3+1
  drift: ^2.18.0

  # ===== Media =====
  audioplayers: ^6.1.0
  video_player: ^2.9.2

  # ===== Connectivity =====
  url_launcher: ^6.3.1

  # ===== Utilities =====
  package_info_plus: ^8.0.0
  share_plus: ^9.0.0
  permission_handler: ^11.3.1
  device_info_plus: ^10.1.0
  app_links: ^6.0.0
  flutter_displaymode: ^0.7.0
  path: ^1.9.0
  shimmer: ^3.0.0
  record: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.0
  drift_dev: ^2.18.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  generate: true

  assets:
    - .env
    - assets/
    - assets/images/
    - assets/fonts/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter/Inter-Regular.ttf
        - asset: assets/fonts/Inter/Inter-Bold.ttf
          weight: 700
